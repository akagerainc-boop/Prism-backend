# Docs Scanner admin dashboard -- environment template
#
#   copy .env.example .env.local
#
# Points the dashboard at the backend API. On Railway, set this as a build
# env var for the admin service (its own deploy, separate from the backend).

VITE_API_BASE_URL=https://prism-backend-9hjw.onrender.com
